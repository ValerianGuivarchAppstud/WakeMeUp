<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@android:style/Widget.ActionButton"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:layout_gravity="center"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical"
    android:background="@color/colorPrimary"
    >


    <ImageView
        app:backgroundTint="@color/colorPrimary"
        android:id="@+id/icone_notif_attente"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:contentDescription="@string/icone_notifications_musiques"
        android:gravity="center"
        app:srcCompat="@drawable/icon_notif_no" />

    <TextView
        android:id="@+id/text_message_notification"
        android:textSize="12sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/icone_notif_attente"
        android:layout_alignEnd="@id/icone_notif_attente"
        android:layout_marginTop="3dp"
        android:layout_marginEnd="0dp"
        android:gravity="center"
        android:minWidth="17sp"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingBottom="1dp"
        android:textColor="#ffffffff"
        android:visibility="invisible" />

</RelativeLayout>