<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@android:style/Widget.ActionButton"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:layout_gravity="center"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical">


    <ImageView
        android:id="@+id/icone_musique_attente"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:contentDescription="@string/icone_notifications_musiques"
        android:gravity="center"
        android:src="@drawable/icon_music_no" />

    <TextView
        android:id="@+id/text_message_notification"
        android:textSize="12sp"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/icone_musique_attente"
        android:layout_alignEnd="@id/icone_musique_attente"
        android:layout_marginTop="3dp"
        android:layout_marginEnd="0dp"
        android:gravity="center"
        android:minWidth="17sp"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingBottom="1dp"
        android:textColor="#ffffffff"
        android:visibility="invisible" />

</RelativeLayout>